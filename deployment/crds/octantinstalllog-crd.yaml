apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: octantinstalllogs.octant.mydecisive.ai
spec:
  group: octant.mydecisive.ai
  names:
    kind: OctantInstallLog
    listKind: OctantInstallLogList
    plural: octantinstalllogs
    singular: octantinstalllog
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: OctantInstallLog is the Schema for the setup logs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OctantInstallLogSpec defines the data schema.
            properties:
              events:
                items:
                  properties:
                    action:
                      description: Action is the type of step in progressing the Octant/MDAI
                        system towards the installed state
                      type: string
                    message:
                      description: Message is an optional message to help further
                        diagnosis of failure/partial-success events
                      type: string
                    namespace:
                      description: Namespace is the namespace this event took place
                        in
                      type: string
                    ref:
                      description: Ref is the integration/connection/other resource
                        that this action targeted
                      type: string
                    result:
                      description: Result is the success/partial-success/failure state
                        of this event
                      type: string
                    subtype:
                      description: |-
                        Subtype is the specific underlying type of the integration/other (Argo for deploy integration, Datadog for
                        destination)
                      type: string
                    timestamp:
                      description: Timestamp is the timestamp of when this state change
                        was observed
                      type: string
                  required:
                  - action
                  - timestamp
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true

