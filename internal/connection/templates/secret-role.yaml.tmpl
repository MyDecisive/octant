apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ .AppName }}-integration-secret-role
  namespace: {{ .Namespace }}
  labels:
    app: {{ .AppName }}
    app.kubernetes.io/instance: {{ .AppName }}
rules:
  - apiGroups: [ "" ]
    resources: [ "secrets" ]
    verbs: [ "get" ]
    resourceNames: ["{{ .AppName }}-integration-secret"]