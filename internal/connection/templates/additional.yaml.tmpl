apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ .AppName }}-octant-secret-access
  namespace: {{ .Namespace }}
  labels:
    app: {{ .AppName }}
    app.kubernetes.io/instance: {{ .AppName }}
  {{- if .IsArgoSideload }}
  annotations:
    argocd.argoproj.io/tracking-id: {{ .AppName }}:rbac.authorization.k8s.io/v1:mdai/{{ .AppName }}-octant-secret-access
  {{- end }}
rules:
  - apiGroups: [ "" ]
    resources: [ "secrets" ]
    verbs: [ "get" ]
    resourceNames: ["greptimedb-users-auth"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ .AppName }}-octant-secret-access-binding
  namespace: {{ .Namespace }}
  labels:
    app: {{ .AppName }}
    app.kubernetes.io/instance: {{ .AppName }}
  {{- if .IsArgoSideload }}
  annotations:
    argocd.argoproj.io/tracking-id: {{ .AppName }}:rbac.authorization.k8s.io/v1:mdai/{{ .AppName }}-octant-secret-access-binding
  {{- end }}
subjects:
  - kind: ServiceAccount
    name: {{ .ServiceAccount }}
    namespace: {{ .CurrentNamespace }}
roleRef:
  kind: Role
  name: {{ .AppName }}-octant-secret-access
  apiGroup: ""